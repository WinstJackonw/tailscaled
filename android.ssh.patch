diff --git a/envknob/featureknob/featureknob.go b/envknob/featureknob/featureknob.go
index 0493665..eab17fe 100644
--- a/envknob/featureknob/featureknob.go
+++ b/envknob/featureknob/featureknob.go
@@ -31,7 +31,7 @@ func CanRunTailscaleSSH() error {
 		if version.IsSandboxedMacOS() {
 			return errors.New("The Tailscale SSH server does not run in sandboxed Tailscale GUI builds.")
 		}
-	case "freebsd", "openbsd", "plan9":
+	case "freebsd", "openbsd", "plan9", "android":
 	default:
 		return errors.New("The Tailscale SSH server is not supported on " + runtime.GOOS)
 	}
diff --git a/feature/ssh/ssh.go b/feature/ssh/ssh.go
index bd22005..6471811 100644
--- a/feature/ssh/ssh.go
+++ b/feature/ssh/ssh.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build ((linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9) && !ts_omit_ssh
+//go:build (linux || (darwin && !ios) || freebsd || openbsd || plan9) && !ts_omit_ssh
 
 // Package ssh registers the Tailscale SSH feature, including host key
 // management and the SSH server.
diff --git a/paths/paths.go b/paths/paths.go
index 398d8b2..8bba364 100644
--- a/paths/paths.go
+++ b/paths/paths.go
@@ -22,6 +22,9 @@
 // DefaultTailscaledSocket returns the path to the tailscaled Unix socket
 // or the empty string if there's no reasonable default.
 func DefaultTailscaledSocket() string {
+	if runtime.GOOS == "android" {
+		return "/data/adb/tailscale/run/tailscaled.sock"
+	}
 	if runtime.GOOS == "windows" {
 		return `\\.\pipe\ProtectedPrefix\Administrators\Tailscale\tailscaled`
 	}
diff --git a/ssh/tailssh/c2n.go b/ssh/tailssh/c2n.go
index 621be74..821e4c2 100644
--- a/ssh/tailssh/c2n.go
+++ b/ssh/tailssh/c2n.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/ssh/tailssh/hostkeys.go b/ssh/tailssh/hostkeys.go
index 8046a02..358e6dd 100644
--- a/ssh/tailssh/hostkeys.go
+++ b/ssh/tailssh/hostkeys.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/ssh/tailssh/incubator.go b/ssh/tailssh/incubator.go
index faf70af..11f0c99 100644
--- a/ssh/tailssh/incubator.go
+++ b/ssh/tailssh/incubator.go
@@ -7,7 +7,7 @@
 // and groups to the specified `--uid`, `--gid` and `--groups`, and
 // then launches the requested `--cmd`.
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd
+//go:build linux || (darwin && !ios) || freebsd || openbsd
 
 package tailssh
 
@@ -52,6 +52,7 @@
 	freebsd = "freebsd"
 	openbsd = "openbsd"
 	windows = "windows"
+	android = "android"
 )
 
 func init() {
@@ -866,6 +867,12 @@ func doDropPrivileges(dlogf logger.Logf, wantUid, wantGid int, supplementaryGrou
 // via an inherited file instead (see forwardedEnvFile).
 func (ss *sshSession) incubatorEnv() []string {
 	env := envForUser(ss.conn.localUser)
+	if runtime.GOOS == "android" {
+		// On rooted Android, append the tailscaled daemon environment so the
+		// SSH session inherits HOME (module sets HOME=/data/ssh/root) and an
+		// Android-appropriate PATH. Restores pre-1.83 upstream behavior.
+		env = append(env, os.Environ()...)
+	}
 	for _, kv := range ss.Environ() {
 		if acceptEnvPair(kv) {
 			env = append(env, kv)
@@ -1220,6 +1227,9 @@ func (ia *incubatorArgs) loginArgs(loginCmdPath string) []string {
 		return []string{loginCmdPath, "-f", ia.localUser, "-h", ia.remoteIP, "-p"}
 	case freebsd, openbsd:
 		return []string{loginCmdPath, "-fp", "-h", ia.remoteIP, ia.localUser}
+	case android:
+		// Android has no usable login(1); do not pass login(1)-specific flags.
+		return []string{loginCmdPath}
 	}
 	panic("unimplemented")
 }
@@ -1237,6 +1247,15 @@ func shellArgs(isShell bool, cmd string) []string {
 }
 
 func setGroups(groupIDs []int) error {
+	if runtime.GOOS == "android" {
+		if os.Geteuid() == 0 {
+			err := syscall.Setgroups(groupIDs)
+			if err != nil {
+				fmt.Println("Setgroups failed:", err)
+			}
+		}
+		return nil
+	}
 	if runtime.GOOS == darwin && len(groupIDs) > 16 {
 		// darwin returns "invalid argument" if more than 16 groups are passed to syscall.Setgroups
 		// some info can be found here:
diff --git a/ssh/tailssh/incubator_linux.go b/ssh/tailssh/incubator_linux.go
index cff4616..3539ffc 100644
--- a/ssh/tailssh/incubator_linux.go
+++ b/ssh/tailssh/incubator_linux.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build linux && !android
+//go:build linux
 
 package tailssh
 
diff --git a/ssh/tailssh/listen.go b/ssh/tailssh/listen.go
index 8c98127..3e72b59 100644
--- a/ssh/tailssh/listen.go
+++ b/ssh/tailssh/listen.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/ssh/tailssh/session.go b/ssh/tailssh/session.go
index c19ae7b..abc7710 100644
--- a/ssh/tailssh/session.go
+++ b/ssh/tailssh/session.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/ssh/tailssh/tailssh.go b/ssh/tailssh/tailssh.go
index 3e3587f..a0c9e9a 100644
--- a/ssh/tailssh/tailssh.go
+++ b/ssh/tailssh/tailssh.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 // Package tailssh is an SSH server integrated into Tailscale.
 package tailssh
@@ -367,11 +367,9 @@ func (c *conn) clientAuth(cm ssh.ConnMetadata) (perms *ssh.Permissions, retErr e
 		if err != nil {
 			return nil, c.errBanner(fmt.Sprintf("failed to look up local user %q ", localUser), err)
 		}
-		gids, err := lu.GroupIds()
-		if err != nil {
-			return nil, c.errBanner("failed to look up local user's group IDs", err)
-		}
-		c.userGroupIDs = gids
+		// On Android, user group lookup fails (no /etc/group); run SSH
+		// sessions as root (uid 0) instead.
+		c.userGroupIDs = []string{"0"}
 		c.localUser = lu
 		c.acceptEnv = acceptEnv
 	}
diff --git a/ssh/tailssh/user.go b/ssh/tailssh/user.go
index 8d7775c..0b2a10d 100644
--- a/ssh/tailssh/user.go
+++ b/ssh/tailssh/user.go
@@ -1,7 +1,7 @@
 // Copyright (c) Tailscale Inc & contributors
 // SPDX-License-Identifier: BSD-3-Clause
 
-//go:build (linux && !android) || (darwin && !ios) || freebsd || openbsd || plan9
+//go:build linux || (darwin && !ios) || freebsd || openbsd || plan9
 
 package tailssh
 
diff --git a/util/osuser/user.go b/util/osuser/user.go
index 3fdbfbb..e348d21 100644
--- a/util/osuser/user.go
+++ b/util/osuser/user.go
@@ -10,6 +10,7 @@
 	"errors"
 	"fmt"
 	"log"
+	"os"
 	"os/exec"
 	"os/user"
 	"reflect"
@@ -106,6 +107,30 @@ func lookup(usernameOrUID string, std lookupStd, wantShell bool) (*user.User, st
 		}, "/bin/rc", nil
 	}
 
+	if runtime.GOOS == "android" {
+		var shell string
+		if wantShell {
+			shell = "/system/bin/sh"
+		}
+
+		homeDir := "/"
+		if out, err := os.UserHomeDir(); err == nil {
+			homeDir = strings.TrimSpace(string(out))
+		}
+
+		u, err := std(usernameOrUID)
+		if err != nil {
+			return &user.User{
+				Uid:      "0",
+				Gid:      "0",
+				Username: "root",
+				Name:     "Android",
+				HomeDir:  homeDir,
+			}, shell, nil
+		}
+		return u, shell, nil
+	}
+
 	// Start with getent if caller wants to get the user shell.
 	if wantShell {
 		return userLookupGetent(usernameOrUID, std)
