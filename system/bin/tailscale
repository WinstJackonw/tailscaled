#!/system/bin/sh
. /data/adb/tailscale/settings.ini
export TS_BE_CLI=1
for arg in "$@"; do
    case "$arg" in
        --socket|--socket=*|-socket|-socket=*) exec "$tailscale_bin" "$@" ;;
    esac
done
exec "$tailscale_bin" $tailscale_bin_param "$@"
