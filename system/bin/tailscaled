#!/system/bin/sh
. /data/adb/tailscale/settings.ini
unset TS_BE_CLI
if [ $# -eq 0 ]; then # if no arguments are passed, start tailscaled
    exec "${tailscaled_bin}" ${tailscaled_bin_param}
else # otherwise, run tailscale with the arguments passed
    exec "${tailscaled_bin}" "$@"
fi
